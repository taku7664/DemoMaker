# 데모 규격

DemoMaker 가 만들어 내는 모든 데모가 지켜야 할 규격입니다. 템플릿을 고르고 내용을 채운 뒤에는
마지막의 점검표를 한 줄씩 확인합니다.

## 1. 공통 전제

- **파일 하나로 완결됩니다.** 외부 스크립트와 글꼴, 이미지를 불러오지 않습니다. 파일을 그냥 두 번
  눌러서 열면 동작해야 하고, 서버나 빌드 과정을 요구하지 않습니다.
- **연출임을 숨기지 않습니다.** 실제로 서버를 부르지 않고 지연만 흉내 낸다면 파일 첫머리 주석과
  본문 마무리 문단에 그 사실을 함께 적습니다. 규칙만 제품과 같게 구현하고, 회귀를 막는 일은
  저장소의 단위 테스트가 맡는다는 것도 밝힙니다.
- **저작권 표시를 둘째 줄에 둡니다.** `<!-- Copyright: DemoMaker (c) 2026 -->`
- **첫머리 주석에 다음 네 가지를 적습니다.** 이 데모가 무엇을 보여 주는지, 어떻게 여는지,
  어디까지가 연출인지, 셀 수 없는 것을 왜 세지 않았는지.

## 2. 형식 고르기

| 상황 | 템플릿 | 특징 |
| --- | --- | --- |
| 조건에 따라 결과가 달라지는 것을 직접 만져 보게 해야 함 | `templates/demo-compare.html` | 조작 막대 + 좌우 비교 |
| 순서가 있는 과정을 보여 주고, 보는 사람이 아무것도 누르지 않아도 됨 | `templates/demo-auto.html` | 열면 자동 재생, 끝난 뒤 기록이 남음 |
| 규칙이 경우마다 어떻게 갈리는지 확인시켜야 함 | `templates/demo-cases.html` | 케이스 표 + 통과·실패 판정 |

한 데모에 두 형식을 섞지 않습니다. 비교와 케이스 표가 모두 필요하다면 파일을 나눕니다.

## 3. 화면의 차례

데모는 아래 순서를 지킵니다. 순서를 바꾸지 않고, 한 자리도 비우지 않습니다.

| 차례 | 자리 | 담는 것 |
| --- | --- | --- |
| 1 | 제목 (`h1`) | 무엇을 다루는지 알 수 있는 짧은 명사구 |
| 2 | 결과물 설명 | 이 화면이 무엇인지 한두 줄 |
| 3 | 목차 (`nav.toc`) | 아래 세 자리로 뛰는 링크 |
| 4 | **왜 필요한가?** (`h2#why`) | 지금 무엇이 문제이고, 누가 어떤 손해를 보며, 바꾸면 무엇이 좋아지는가 |
| 5 | `<hr id="demo">` | 데모 구현부가 여기서 시작한다는 것을 선으로 알립니다 |
| 6 | 조작 항목 | 보는 사람이 조건을 바꾸는 자리. 조작이 필요 없는 데모라면 재생 단추만 둡니다 |
| 7 | 데모 | 좌우 비교 또는 한 칸 무대 |
| 8 | `<hr>` | 데모 구현부가 여기서 끝난다는 것을 선으로 알립니다 |
| 9 | **체크리스트** (`h2#checklist`) | 직접 확인할 것과 눌러 볼 것 |

절을 눈에 보이게 갈라 두는 것도 규격입니다. `h2` 는 본문보다 확실히 크게 두고(18px·굵게), 그 아래
내용은 한 칸 들여씁니다. 크기가 본문과 같으면 절 제목이 그냥 문단으로 읽힙니다. 절 제목에는 머리
기호를 붙이지 않고 소제목만 적습니다. 조작 항목과 데모는 글이 아니라 만져 보는 자리이므로 위아래를
`<hr>` 로 끊어 글과 구분합니다.

### 목차

대제목과 결과물 설명 바로 아래에 목차를 둡니다. 데모는 한 화면에 들어가지 않을 만큼 길어지기 쉬운데,
보는 사람이 「왜 필요한가」만 읽으러 왔을 수도 있고 곧바로 만져 보러 왔을 수도 있기 때문입니다.

**파란 밑줄 글씨를 쓰지 않습니다.** 문서 안에서 자리를 옮기는 것뿐인데 바깥으로 나가는 링크처럼 보일
이유가 없습니다. 대신 항목마다 작은 링크 아이콘을 인라인 SVG 로 붙이고, 글자는 흐린 색으로 두었다가
가리키면 강조색으로 바뀌게 합니다. 눌러서 뛰었을 때 절 제목이 화면 맨 위에 딱 붙지 않도록
`scroll-margin-top` 도 함께 줍니다.

```html
<nav class="toc">
  <a href="#why"><svg …/>왜 필요한가</a>
  <a href="#demo"><svg …/>데모</a>
  <a href="#checklist"><svg …/>체크리스트</a>
</nav>
```

### 제목과 파일 이름

제목은 **무엇을 다루는지 알 수 있는 짧은 명사구**로 씁니다. 목록에서 한눈에 구분되고 회의에서
그대로 부를 수 있어야 하므로, **열다섯 자 안팎**으로 줄입니다. 문제를 설명하는 일은 바로 아래의
「왜 필요한가?」가 맡습니다. 제목에서 미리 설명하려 들면 문장이 길어져 제목 구실을 못 합니다.

| 이렇게 쓰지 않습니다 | 이렇게 씁니다 |
| --- | --- |
| 그래프를 여는 4초 동안 화면은 아무 말도 하지 않는다 | 그래프 로딩 UX 개선 |
| 마지막 한 건을 읽을 때까지 화면은 한 줄도 보여 주지 않는다 | 목록 로딩 UX 개선 |
| 사이드바가 목록을 기다리느라 도구까지 뒤늦추는 문제 | 트리 펼침 속도 개선 |

**파일 이름은 제목과 같게 짓습니다.** 확장자만 붙이고 그 밖의 말을 덧대지 않습니다. 파일 목록과
화면의 제목이 어긋나면 어느 파일이 어느 데모인지 헷갈리기 때문입니다. `<title>` 도 같게 둡니다.

```
그래프 로딩 UX 개선.html
```

### 왜 필요한가?

**이 절이 없는 데모는 내보내지 않습니다.** 무엇이 어떻게 움직이는지만 보여 주는 화면은 절반만 된
것입니다. 보는 사람이 「그래서 왜 바꿔야 하는가」에 답을 얻지 못하면 설득에 쓸 수 없습니다.

세 가지를 적습니다.

1. **지금 무엇이 문제인가** — 현재 동작을 사실대로 적습니다.
2. **그것이 누구에게 어떤 손해인가** — 사용자가 무엇을 못 하게 되는지, 어떤 오해를 하게 되는지 적습니다.
3. **바꾸면 무엇이 좋아지는가** — 여기서 과장하지 않습니다. 줄지 않는 것은 줄지 않는다고 적습니다.

> **모르면 지어내지 말고 물어봅니다.** 코드를 분석해도 왜 이 변경이 필요한지 알 수 없다면, 그럴듯한
> 이유를 만들어 채우지 말고 요청한 사람에게 물어서 확인합니다. 배경을 짐작으로 채운 데모는 나머지
> 내용까지 믿을 수 없게 만듭니다.

### 체크리스트

마무리 자리에는 문단 대신 **보는 사람이 직접 확인할 목록**을 둡니다. 각 줄은 체크 상자 하나와 설명
한 단으로 이루어집니다. 두 가지를 섞어 씁니다.

- **확인할 것** — 「왼쪽은 끝날 때까지 문구가 한 글자도 바뀌지 않습니다.」
- **눌러 볼 것** — 「재시도를 5회로 올려 보십시오.」

설명에는 **왜 그렇게 만들었는지**와 **이 화면의 한계**를 적습니다. 연출이라는 사실과 회귀는 단위
테스트가 맡는다는 것도 이 목록의 마지막 줄에 둡니다.

### 글의 세부

- 좌우 비교에서는 왼쪽이 항상 「바뀌기 전」, 오른쪽이 「바뀐 뒤」입니다.
- 셀 수 없는 것은 세지 않습니다. 총계를 미리 알 수 없는 구간에는 분모를 붙이지 말고 누적 수만 적습니다.
- 조작 항목은 결과가 눈에 띄게 달라지는 것만 남기고 넷을 넘기지 않습니다.

### 번역투를 쓰지 않습니다

번역투는 **문장 구조**의 문제이지 낱말의 문제가 아닙니다. 영어 낱말이 섞여 있어도 한국어 문장으로
읽히면 괜찮고, 전부 한글이어도 영어 구조를 베꼈으면 고쳐야 합니다. 그러므로 「영어 낱말이 있는가」가
아니라 **「서술어가 있고 행위의 주체가 보이는가」** 로 판단합니다.

**금지 — 아래 구조는 쓰지 않습니다**

| 쓰지 않는 구조 | 고친 문장 |
| --- | --- |
| 데이터가 로드되어집니다 | 목록이 도착합니다 |
| 사용자에 의해 선택된 항목 | 사용자가 고른 항목 |
| 3개의 항목을 가집니다 | 항목이 3개입니다 |
| 로딩에 대한 처리를 수행합니다 | 읽는 동안을 처리합니다 |
| 즉각적인 피드백을 제공합니다 | 무엇이 끝났는지 바로 알려 줍니다 |
| 확인하는 것이 가능합니다 | 확인할 수 있습니다 |
| 이 데모는 당신에게 차이를 보여 줍니다 | 이 화면에서 차이를 볼 수 있습니다 |
| 전체 데이터 로드 후 렌더링 처리 | 전부 읽고 한 번에 그린다 |

고치는 요령은 둘입니다.

1. **주체를 밝히고 능동으로 씁니다.** 무엇이 무엇을 하는지 적으면 피동과 「~에 의해」가 저절로
   사라집니다.
2. **동사로 끝냅니다.** 명사를 석 줄 쌓지 말고 서술어를 놓습니다. 다만 조작 항목의 이름표나 표의
   머리글처럼 짧은 이름 자리는 명사구로 두어도 됩니다.

**허용 — 낱말은 막지 않습니다**

다음은 그대로 씁니다. 억지로 옮기면 오히려 알아보기 어렵습니다.

- 그 분야에서 쓰는 용어: LSP, API, JSON, 커밋, 빌드
- 화면에서 굳어진 짧은 이름표: Before, After, PASS, FAIL
- 우리말과 외래어가 모두 통하는 말(로딩·읽기, 렌더링·그리기)은 어느 쪽을 골라도 되지만,
  **한 데모 안에서는 한 쪽으로 통일합니다.** 같은 것을 두 이름으로 부르면 보는 사람이 다른 것인 줄
  압니다.

## 4. 코드

- `<script>` 전체를 즉시 실행 함수로 감싸고 첫 줄에 `'use strict';` 를 둡니다.
- 익명 함수 대신 이름 있는 함수를 씁니다. 기록과 오류 화면에 이름이 남습니다.
- 규칙이 걸린 자리에는 **왜 그렇게 했는지**를 한국어 주석으로 남깁니다.
- 타이머는 모두 배열에 모아 두고 초기화할 때 한꺼번에 해제합니다. 해제하지 않으면 다시 실행할 때
  이전 재생이 겹쳐 보입니다.
- 사용자에게 보이는 문자열을 HTML 로 조립할 때에는 반드시 이스케이프를 거칩니다.

## 5. 어떤 상황에서도 깔끔하게

데모는 잘 되는 경우만 보여 주면 안 됩니다. 아래 다섯 가지 상태를 모두 그려야 합니다.

1. **아직 실행하지 않음** — 빈 상자 대신 「무엇을 누르면 되는지」를 문장으로 둡니다.
2. **기다리는 중** — 곧 채워질 자리를 뼈대(`.skel`, `.placeholder`)로 미리 잡습니다. 자리를 잡아
   두어야 내용이 도착할 때 화면이 밀리지 않습니다.
3. **보여 줄 것이 없음** — 항목이 0건이어도 빈 화면을 남기지 말고 그 사정을 말합니다.
4. **실패** — 지금까지 받은 것은 남겨 두고, 멈춘 이유를 덧붙입니다.
5. **완료** — 조작을 멈춘 뒤에도 결과가 그대로 남아, 화면을 갈무리하면 그것만으로 설명이 됩니다.

레이아웃이 무너지지 않게 하는 장치도 함께 지킵니다.

- 로그와 목록 상자는 높이를 고정하고 안에서만 넘치게 둡니다. 줄이 아무리 쌓여도 문서가 늘어나지 않습니다.
- 좌우 두 칸의 설명 길이가 달라도 아래 상자가 어긋나지 않도록 `.sub` 에 `min-height` 를 줍니다.
- 수치 자리에는 값이 없을 때에도 `—` 를 넣어 높이를 유지합니다.
- `overflow-wrap:anywhere` 와 `word-break:keep-all` 로 긴 경로가 들어와도 가로 스크롤이 생기지 않게 합니다.
- 밝은 화면과 어두운 화면을 모두 지원하고, `prefers-reduced-motion` 에서는 애니메이션을 끕니다.

## 6. 내보내기 전 점검표

- [ ] 제목이 열다섯 자 안팎의 짧은 명사구입니까?
- [ ] 파일 이름이 제목과 같습니까?
- [ ] 「왜 필요한가?」 절이 있고, 문제·손해·바뀐 뒤 셋을 모두 적었습니까?
- [ ] 왜 필요한지를 짐작으로 채우지 않고 확인한 사실로 적었습니까?
- [ ] 대제목 아래에 목차가 있고, 세 자리로 모두 뛰어집니까?
- [ ] 마무리가 문단이 아니라 체크리스트입니까?
- [ ] 조작 항목과 데모의 위아래에 구분선이 있습니까?
- [ ] 파일을 두 번 눌러 열었을 때 그대로 동작합니까?
- [ ] 브라우저 콘솔에 오류가 하나도 없습니까?
- [ ] 폭을 360px 까지 줄였을 때 가로 스크롤 없이 한 칸으로 접힙니까?
- [ ] 어두운 화면에서 모든 글자와 테두리가 읽힙니까?
- [ ] 아무것도 누르지 않은 첫 화면이 이미 무언가를 말하고 있습니까?
- [ ] 빈 상태와 실패 상태를 직접 만들어 보았고, 둘 다 문장으로 사정을 말합니까?
- [ ] 다시 실행했을 때 이전 재생이 겹치지 않습니까?
- [ ] 체크리스트 마지막 줄에 이 화면이 연출이라는 사실과 그 한계가 적혀 있습니까?
- [ ] 셀 수 없는 것에 분모를 붙이지 않았습니까?
- [ ] 화면의 문구를 소리 내어 읽었을 때, 동료에게 그대로 말하지 않을 문장이 없습니까?
- [ ] 같은 것을 두 이름으로 부른 자리가 없습니까?
