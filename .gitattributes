# Auto detect text files and perform LF normalization
* text=auto

# 설치 스크립트는 실행 환경이 정해져 있으므로 줄 끝을 고정합니다.
# install.ps1 은 Windows PowerShell 5.1 이 한글을 바르게 읽도록 UTF-8 BOM 을 유지해야 합니다.
*.ps1 text eol=crlf
*.sh  text eol=lf
